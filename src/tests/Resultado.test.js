import React from "react";
import { render } from "@testing-library/react";
import Resultado from "../pages/Resultado/Resultado";

describe("Página Resultado.js", () => {
  it("", () => {
    const { getAllByRole } = render(<Resultado />);

  });
});